package com.example.myautoo.uiTest.components.feature.auth
/*
import androidx.compose.ui.test.assertIsDisplayed
import androidx.compose.ui.test.junit4.createComposeRule
import androidx.compose.ui.test.onNodeWithContentDescription
import androidx.compose.ui.test.onNodeWithText
import com.example.myautoo.ui.feature.auth.AuthHeader
import org.junit.Rule
import org.junit.Test

class AuthHeaderTest {

    @get:Rule
    val composeTestRule = createComposeRule()

    @Test
    fun authHeader_DisplaysLogoAndSubTitle() {
        // 1. Arrange: Define el estado de prueba
        val subTitle = "Bienvenido de nuevo"

        // 2. Act: Renderiza el componente en el entorno de prueba
        composeTestRule.setContent {
            AuthHeader(subTitle = subTitle)
        }

        // 3. Assert: Verifica que la UI se muestra como se espera
        // Comprueba que el subtítulo es visible
        composeTestRule.onNodeWithText(subTitle).assertIsDisplayed()

        // Comprueba que el logo es visible buscándolo por su descripción de contenido
        composeTestRule.onNodeWithContentDescription("App Logo").assertIsDisplayed()
    }
}
*/

